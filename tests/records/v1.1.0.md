# v1.1.0 驗證記錄

- **日期**：2026-09-07
- **版號**：1.1.0（minor，使用者指定）

## 需求描述

準備發佈到 npm（進而收錄至 Joplin 官方外掛倉庫）：

1. 新增發佈 script
2. 更新 README.md（舊版描述 v1.0.2 時代功能，已過時）
3. 版號更新至 1.1.0

## 變更摘要

- `package.json`：
  - 補 `description`（與 manifest 一致；npm 頁面沿用舊版過時文案）
  - 新增 `repository`（npm 頁面來源連結）
  - 新增 `"publish": "npm publish"` script — `npm run publish` 即發佈；npm publish 自動觸發既有 `prepare` → `npm run dist` 重新打包 publish/（.jpl + .json）
  - `version` → 1.1.0
- `src/manifest.json`：
  - `repository_url` / `homepage_url`：`https://github.com/linuxxunil`（個人首頁）→ `https://github.com/linuxxunil/joplin-plugin-note-ai`（實際 repo，取自 git origin）
  - `version` → 1.1.0
- `README.md`：重寫 — 功能（魔法棒單頁流程/重新生成/測試連線/處理筆記/對話）、Provider 表格（10 家、OpenCode Go session 標頭、各供應商分別記憶）、安裝方式（搜尋/手動 .jpl）、開發與發佈說明

## 驗證項目

| 項目 | 結果 |
| --- | --- |
| `npm run dist`（TypeScript 編譯 + 打包） | PASS（bump 前、後各一次） |
| 產物版號檢查（`tar -xOf publish/vip.bestsvip.note-ai.jpl manifest.json`） | PASS — 內嵌 `"version": "1.1.0"`，與 `package.json`、`src/manifest.json` 三者一致 |
| 產物 manifest repository_url 檢查 | PASS — 指向實際 repo |
| 安全審查：中繼資料/文件變更，無程式碼變更、無 secrets、無新輸入面 | PASS |

## 實機檢核（待使用者確認）

- [ ] `git push` 成功（origin: github.com/linuxxunil/joplin-plugin-note-ai）
- [ ] `npm login`（帳號 linuxxunil）後 `npm run publish` 成功，npm 上出現 1.1.0
- [ ] Joplin 設定 → 外掛程式 可搜尋並安裝「Note AI」（官方掃描有延遲，非即時）

# 驗證記錄 — v1.0.4

- **日期**：2026-09-07
- **版號**：1.0.4（package.json + src/manifest.json 同步）

## 需求描述

1. 對話框標題「Note AI 魔法棒」改為「Note AI」。
2. 調大對話框視窗（90vw / 80vh）。
3. 按鈕語義調整：
   - 生成：將輸入內容送給 LLM，結果輸出至另一個 Text。
   - 寫入：將生成內容寫入 Note（附加至筆記末尾）。
   - 取消：放棄當前功能。

## 變更摘要

- `src/magicWand.ts`：
  - 兩階段視窗標題統一為「Note AI」；兩階段皆使用 `setFitToContent(handle, false)`（90vw/80vh 大視窗）。
  - 階段 1 按鈕：「取消 / 生成」。
  - 階段 2 顯示「輸入內容」唯讀 Text 與「生成結果」可編輯 Text（寫入前可修改），按鈕：「取消 / 寫入」。
  - 寫入 = 附加至筆記末尾（寫入前重讀 note body 防競態）；移除先前的「取代全文 / 插入游標處」分支。
- `src/index.ts`：指令顯示名稱改為「Note AI: 整理筆記 / 優化內容」。

## 驗證項目與結果

| 項目 | 結果 |
| --- | --- |
| `npm run dist`（webpack + TypeScript 編譯 + 打包 .jpl） | PASS |
| 安全檢查：階段 2 動態內容（source / result）HTML escape | PASS — 皆經 `escapeHtml()` |
| 安全檢查：寫入路徑競態防護（重讀 body） | PASS |
| 安全檢查：secrets / 注入 / 依賴 | PASS — 無新增 log、無新增依賴 |
| 版號同步：package.json = manifest.json = 1.0.4 | PASS |

# 驗證記錄 — v1.0.11

- **日期**：2026-09-07
- **版號**：1.0.11（package.json + src/manifest.json + publish/*.jpl 內嵌版號，三者一致）

## 需求描述

1. 移除編輯器工具列上的紅色 🔄 圖示（`noteAiApplyEndpoint` 套用端點按鈕）。
2. 「更新」按鈕改放到設定頁 API Base URL 旁邊 — **平台限制已查證確認不可行**：Joplin 設定頁 `handleSettingButton` 硬編碼僅處理 4 個內部按鈕（`sync.openSyncWizard` 等），外掛 Button 型別設定被點擊時直接拋出 `Unhandled key` 錯誤（ConfigScreen.tsx 原始碼 + 2023 論壇討論佐證），外掛設定 API 亦不傳遞 `onClick`。

## 變更摘要

- 移除 `noteAiApplyEndpoint` 的 EditorToolbar 按鈕註冊（`toolbarButtons.create` 僅剩魔法棒）。
- 保留功能入口：指令「Note AI: 套用所選 Provider 的 API 端點」仍在 **Tools 選單**與**指令面板**。
- 即時回饋維持：切換 Provider 時 Toast 顯示「已載入「○○」設定 — 端點：{url}」；執行「套用端點」亦以 Toast 顯示結果。
- URL 欄位說明修正：移除「工具列」字樣，改註明 Tools 選單／指令面板與 Joplin 重繪限制。

## 替代方案說明（設定頁按鈕不可行的補償）

- 資料層自動套用正常：切換 Provider 即自動寫入正確端點（H1 已驗證），LLM 呼叫使用正確設定。
- 視覺回饋：切換與套用均有 Toast 顯示實際端點。
- 手動套用：Tools 選單／指令面板「套用端點」。

## 驗證項目與結果

| 項目 | 結果 |
| --- | --- |
| `npm run dist`（webpack + TypeScript 編譯 + 打包 .jpl） | PASS |
| 產物版號檢查：package.json = manifest.json = jpl 內嵌 = 1.0.11 | PASS |
| 產物確認：`toolbarButtons.create` 僅魔法棒 1 處；`noteAiApplyEndpoint` 僅存於指令 + Tools 選單 | PASS |
| 安全安全檢查：純 UI 入口調整，無 secrets / 注入面 / 新依賴 | PASS |
| 版號同步：package.json = manifest.json = 1.0.11 | PASS |

## 實機驗收待確認

- 編輯器工具列僅剩魔法棒一顆按鈕（紅色 🔄 已消失）。
- Tools 選單仍有「套用所選 Provider 的 API 端點」；執行後通知顯示套用結果。

# v1.1.1 驗證記錄

- **日期**：2026-09-07
- **版號**：1.1.1

## 需求描述

新增獨立的 npm 發佈步驟說明文件（`publish.md`），內容專注步驟；README 加連結；版號 1.1.1（1.1.0 未發佈到 npm，跳號無衝突）。

## 變更摘要

- 新增 `publish.md`：發佈步驟（版號同步 → 建 token（Automation 型）→ `npm publish --//registry.npmjs.org/:_authToken=…` → npm 頁面確認 → 官方外掛倉庫自動收錄；含 token 安全注意一行）。屬維護者文件，不會打包進 npm tarball（`files: ["publish"]` 僅含 publish/ 目錄，與 publish.md 檔名不衝突）
- `README.md`：開發段加「發佈步驟詳見 publish.md」連結
- `package.json` / `src/manifest.json`：version → 1.1.1

## 驗證項目

| 項目 | 結果 |
| --- | --- |
| `npm run dist`（TypeScript 編譯 + 打包） | PASS（bump 前、後各一次） |
| 產物版號檢查（`tar -xOf publish/vip.bestsvip.note-ai.jpl manifest.json`） | PASS — 內嵌 `"version": "1.1.1"`，與 `package.json`、`src/manifest.json` 三者一致 |
| 安全審查：文件類變更，token 僅佔位符（`npm_你的token`）無實值；無 secrets、無程式碼變更 | PASS |

## 實機檢核（待使用者確認）

- [ ] 使用者以 token 執行 `npm publish --//registry.npmjs.org/:_authToken=npm_你的token`，npm 上出現 1.1.1
- [ ] Joplin 設定 → 外掛程式 搜尋「Note AI」確認官方收錄（掃描有延遲）

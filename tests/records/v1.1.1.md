# v1.1.1 驗證記錄

- **日期**：2026-09-07
- **版號**：1.1.1（patch；1.1.0 已準備發佈但未成功上傳 npm，跳號無衝突）

## 需求描述

1. README 新增「發佈到 npm」步驟說明（使用者指定：使用方式 A 一次性 token 旗標發佈，內容精簡為純步驟）
2. 版號更新至 1.1.1

## 變更摘要

- `README.md`：「開發」章節改為「開發與發佈」— 開發（install/dist）＋「發佈到 npm」四步驟：
  1. 更新版號（package.json 與 src/manifest.json 一致）
  2. npmjs.com 建立發佈用 token（Automation 類型）
  3. `npm publish --//registry.npmjs.org/:_authToken=npm_你的token`
  4. npm 頁面確認新版本
- `package.json` / `src/manifest.json`：version → 1.1.1

## 驗證項目

| 項目 | 結果 |
| --- | --- |
| `npm run dist`（TypeScript 編譯 + 打包） | PASS |
| 產物版號檢查（`tar -xOf publish/vip.bestsvip.note-ai.jpl manifest.json`） | PASS — 內嵌 `"version": "1.1.1"`，與 `package.json`、`src/manifest.json` 三者一致 |
| 安全審查：文件類變更；README 僅含 token 佔位符（無實際 token 值） | PASS |

## 實機檢核（待使用者確認）

- [ ] 執行 `npm publish --//registry.npmjs.org/:_authToken=npm_你的token` → npm 上出現 1.1.1
- [ ] Joplin 設定 → 外掛程式 搜尋「Note AI」確認官方收錄（掃描有延遲）
